/* global chrome */
import React, { useState } from "react";
import Box from "../Box";

const FormObligatorioExclusivo = () => {
  const [count, setCount] = useState(0);

  return <Box> Hola</Box>;
};

export default FormObligatorioExclusivo;
